import * as AspectRatio from '@radix-ui/react-aspect-ratio'
import { ComponentFactory } from '../../../types'

export const composeAspectRatio = ({ styled }: ComponentFactory) => styled(AspectRatio.Root, {})
